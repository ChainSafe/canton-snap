/**
 * Shared constants. Kept in one file so the schema version and other
 * identifiers can't drift between the validator, the dialog renderer,
 * and the test fixtures.
 */

export const PREPARED_TRANSACTION_SCHEMA = "canton-snap.prepared-transaction.v1";
